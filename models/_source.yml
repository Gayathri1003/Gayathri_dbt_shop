version: 2

sources:
  - name: src_shop
    database: DBT_GAYATHRI_SHOP
    schema: RAW_SCHEMA_SHOP

    tables:
      - name: customers

      - name: products

      - name: orders
        config:
          loaded_at_field: order_date
          freshness:
            warn_after:
              count: 1
              period: day
            error_after:
              count: 2
              period: day

      - name: order_items

      - name: payments
